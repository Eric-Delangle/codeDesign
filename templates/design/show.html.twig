{% extends 'base.html.twig' %}

{% block title %}Design
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<table class="table mt-3">
			<tbody>
				<tr>
					<th></th>
					<td class="text-center">
						<strong class="intro title-size">{{ design.name }}</strong>
					</td>
				</tr>
				<tr>
					<th></th>
					<td class="text-center">
						<a href="{{ path('design_show', {'slug': design.slug}) }}"><img src=" {{ asset(['images/photos/', design.picture]|join) }} " class="images-grandes" alt="design"></a>
					</td>
				</tr>
				<tr>
					<th>Description :</th>
					<td>{{ design.description }}</td>
				</tr>
			</tbody>
		</table>
		<div class="container text-center">
			<button class="btn btn-success">
				<a href="{{ path('design_index') }}" class="bouton">retour à la liste</a>
			</button>
			<button class="btn btn-success">
				<a href="{{ path('home') }}" class="bouton">Accueil</a>
			</button>
		</div>
	</div>

{% endblock %}
