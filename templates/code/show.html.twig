{% extends 'base.html.twig' %}

{% block title %}Code
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<table class="table mt-3">
			<tbody>
				<tr>
					<th></th>
					<td class="text-center">
						<strong class="intro title-size">{{ code.name }}</strong>
					</td>
				</tr>
				<tr>
					<th></th>
					<td class="text-center">
						<a href="{{ path('code_show', {'slug': code.slug}) }}"><img src=" {{ asset(['images/photos/', code.picture]|join) }} " class="images-grandes" alt="code"></a>
					</td>
				</tr>
				<tr>
					<th>Description :</th>
					<td>{{ code.description }}</td>
				</tr>
				<tr>
					<th>Lien :</th>
					<td>
						<a href="{{code.link}} ">{{ code.link }}</td>
					</td>
				</tr>
			</tbody>
		</table>
		<div class="container text-center">
			<button class="btn btn-success">
				<a href="{{ path('code_index') }}" class="bouton">retour à la liste</a>
			</button>
			<button class="btn btn-success">
				<a href="{{ path('home') }}" class="bouton">Accueil</a>
			</button>
		</div>
	</div>

{% endblock %}
