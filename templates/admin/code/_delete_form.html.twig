<form method="post" action="{{ path('admin_code_delete', {'id': code.id}) }}" onsubmit="return confirm('Tu es sur de vouloir supprimer ce site ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
